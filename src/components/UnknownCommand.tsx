import React from "react";

const UnknownCommand: React.FC = () => {
    return <>Unknown command. Type <span className='text-blue-500 dark:text-yellow-500'>help</span> to see available commands.</>
}

export default UnknownCommand